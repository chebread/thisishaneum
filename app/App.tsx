import Router from 'components/Router/Router';
import FullScreen from 'layouts/Screens/FullScreen';

const App = () => {
  return (
    <FullScreen>
      <Router />
    </FullScreen>
  );
};

export default App;
